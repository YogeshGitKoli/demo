name: Demo-1 Workflow

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: test demo1
        run: echo "Hello, Yogesh in GitHub Actions!"

